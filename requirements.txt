# core (offline / mock mode needs only these)
pydantic>=2.5
requests>=2.31

# UI
streamlit>=1.36

# real LLM path (install the one you use)
anthropic>=0.40 ; extra == "anthropic"
openai>=1.40 ; extra == "openai"

# tests
pytest>=8.0
